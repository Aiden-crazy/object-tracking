# -*- coding: utf-8 -*-
"""
report_part1.py —— 报告内容·第一部分
封面 / 评分表 / 团队成员 / 摘要 / 第1章 系统目标与分析 / 第2章 项目管理
作者：【姓名】  学号：【学号】  创建时间：2026-07
"""
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.shared import Pt

from docx_lib import (EA_HEI, add_body, add_center_title, add_code, add_fig,
                      add_h1, add_h2, add_h3, add_para, add_table, add_toc,
                      new_doc, _set_rfonts)

ROOT = None  # 由 gen_report.py 注入


def F(name):
    return ROOT + "/demo/figures/" + name


def S(name):
    return ROOT + "/demo/screenshots/" + name


# ============================ 封面 ============================
def build_cover(doc):
    for _ in range(2):
        doc.add_paragraph()
    add_center_title(doc, "综合实践III课程设计报告", 26)
    p = doc.add_paragraph()
    p.alignment = WD_ALIGN_PARAGRAPH.CENTER
    run = p.add_run("——单目标跟踪系统的设计与实现——")
    run.font.size = Pt(16)
    _set_rfonts(run._element.get_or_add_rPr(), EA_HEI)

    doc.add_paragraph()
    doc.add_paragraph()

    info = [
        ("学    院", "计算机科学与工程学院"),
        ("专    业", "计算机科学与技术"),
        ("班    级", "【班级】"),
        ("学生姓名", "【姓名】"),
        ("学    号", "【学号】"),
        ("指导老师", "罗颂、罗娅、贺筠"),
        ("时    间", "2026 年 7 月"),
    ]
    for label, value in info:
        p = doc.add_paragraph()
        p.alignment = WD_ALIGN_PARAGRAPH.CENTER
        p.paragraph_format.space_after = Pt(10)
        r1 = p.add_run(label + "　")
        r1.font.size = Pt(16)
        _set_rfonts(r1._element.get_or_add_rPr(), EA_HEI)
        r2 = p.add_run("　" + value + "　")
        r2.font.size = Pt(16)
        _set_rfonts(r2._element.get_or_add_rPr(), EA_HEI)
        r2.underline = True
    doc.add_page_break()


# ============================ 评分表 ============================
def build_score(doc):
    add_h1(doc, "课程设计评分表", page_break=False)
    rows = [
        ["课程目标3：能够运用科学方法，对计算机领域的复杂工程问题进行需求和功能分析。",
         "评价标准：具有多角度全面性的局限性和可行性分析。对照多种方案进行方案优选或改进。", "50%", ""],
        ["课程目标5：能够理解多学科背景下的团队中每个角色的定位与责任，能够与团队的成员进行有效沟通，并胜任个人承担的角色任务。",
         "评价标准：有规范、详细、完整的项目分析，项目功能设计完善。", "25%", ""],
        ["课程目标6：能够实施团队工作的过程管理，针对计算机应用系统的分析、设计、开发、测试等环节制定、实施工作计划，能够胜任负责人的角色。",
         "评价标准：有明确的小组分工，有完整的项目管理过程和管理证明材料。", "12.5%", ""],
        ["课程目标7：理解多学科复杂工程问题的知识融合理念，能够组织和管理计算机软硬件项目，并在项目设计、开发等工程实践中，认知或实施质量检测、安全保障，认知复杂系统安全预案的必要性。",
         "评价标准：有完整的项目测试记录及对应的测试结果分析，有全面客观的项目质量评定。", "12.5%", ""],
        ["合计", "", "", ""],
        ["评阅教师（签名或签章）", "", "", ""],
    ]
    add_table(doc, "", ["课程设计报告评分依据", "成绩占比", "成绩"],
              [[r[0] + "｜" + r[1], r[2], r[3]] for r in rows])
    doc.add_paragraph()


# ============================ 团队成员 ============================
def build_team(doc):
    add_h1(doc, "团队成员", page_break=False)
    add_body(doc, "本项目以两人团队方式完成，团队所有成员信息如下（团队角色为组长或成员，"
                  "整个团队不超过2人）：")
    add_table(doc, "", ["学号", "姓名", "团队角色", "团队分工"],
              [
                  ["【学号1】", "【姓名1·本人】", "组长",
                   "视觉跟踪算法设计实现（Python+OpenCV）；Web后端设计与实现"
                   "（Spring Boot）；三端集成与系统测试；报告撰写"],
                  ["【学号2】", "【姓名2】", "成员",
                   "微信小程序端设计与实现；Web管理端页面设计与实现；"
                   "数据库设计；测试用例编写与答辩PPT"],
              ])
    add_body(doc, "注：请打印前删除本行说明，并按实际情况填写上方学号、姓名与分工。")
    doc.add_page_break()


# ============================ 摘要 ============================
def build_abstract(doc):
    p = doc.add_paragraph()
    p.alignment = WD_ALIGN_PARAGRAPH.CENTER
    r = p.add_run("摘    要")
    r.bold = True
    r.font.size = Pt(16)
    _set_rfonts(r._element.get_or_add_rPr(), EA_HEI)
    add_body(doc,
             "针对课程设计提出的“小程序端提交视频、服务端完成视觉处理、Web端进行管理”的"
             "三端联动应用场景，本文设计并实现了一个基于 OpenCV 的智能单目标跟踪系统。"
             "系统以微信小程序为用户端，提供用户注册登录、视频上传与处理结果回看等功能；"
             "以 Spring Boot + MyBatis 为 Web 后端，负责用户认证（JWT）、任务调度与文件存储，"
             "并为管理员提供基于浏览器的 Web 管理端，实现对用户的增删改查、任务记录查询与系统统计；"
             "以 Python + OpenCV 为核心视觉处理层，采用 CSRT 相关滤波跟踪器实现目标的持续跟踪，"
             "并针对目标被遮挡后丢失的问题，设计了“直方图质量校验 + 多尺度模板匹配 + 连续帧确认”"
             "的遮挡检测与自动重检测恢复机制：当目标被短暂遮挡后重新出现时，系统能够再次捕捉目标"
             "并继续跟踪。经功能测试与遮挡恢复专项测试，系统各模块功能正确，在 640×480 分辨率视频上"
             "处理速度约 95~101 帧/秒，完全遮挡恢复场景中目标丢失 1 次并成功重新捕获 1 次，"
             "验证了系统设计的可行性与稳定性。"
             )
    add_body(doc,
             "本文遵循软件工程规范，从系统目标与分析、项目管理、总体设计与详细设计、系统实现、"
             "系统测试与总结等方面，完整记录了项目的分析、设计、实现、测试与总结全过程。")
    p = doc.add_paragraph()
    r = p.add_run("关键词：单目标跟踪；OpenCV；遮挡重检测；微信小程序；Spring Boot")
    r.bold = True
    r.font.size = Pt(12)


# ============================ 第1章 ============================
def build_ch1(doc):
    add_h1(doc, "1 系统目标与分析")
    # ---------------- 1.1 ----------------
    add_h2(doc, "1.1 系统目标")
    add_body(doc,
             "本课程是计算机科学与技术专业的专业实践类课程，课程设计的目的是锻炼学生解决复杂工程"
             "问题的能力，使学生通过完成指定或自选的项目践行软件工程思想，经历完整的问题分析、设计、"
             "实现、测试以及项目实施过程管理与总结的循环。根据《综合实践III》任务书要求，课题需同时"
             "涉及 Web 开发（管理端）、移动开发（小程序端）与机器视觉（核心视觉处理）三个方面，系统"
             "流程为：用户从小程序端登录，提交要处理的图片或视频，服务器端的视觉处理模块对素材进行"
             "处理并返回结果，提交素材与处理结果保存至数据库，管理员从 Web 管理端登录后可对用户进行"
             "增删改查等管理操作。")
    add_body(doc,
             "本项目选择任务书视觉场景中的“单目标跟踪”方向，确定系统目标如下：")
    for t in [
        "（1）完成一套三端联动的单目标跟踪系统：微信小程序端面向普通用户，提供注册、登录、"
        "修改密码、上传跟踪视频、查看处理结果与历史记录功能；",
        "（2）Web 管理端面向管理员，提供用户查询、新增、删除、重置密码，以及任务记录查询、"
        "删除和系统统计功能；",
        "（3）服务器端视觉处理模块基于 Python + OpenCV 实现单目标跟踪：用户指定视频首帧中的目标"
        "对象后，系统持续跟踪并绘制其运动轨迹；当目标被短暂遮挡后再次出现在画面中时，系统能依据"
        "目标特征重新捕捉目标并继续跟踪，即具备“遮挡丢失—自动重检测—恢复跟踪”能力；",
        "（4）提交的视频与跟踪结果（结果视频、轨迹日志、统计信息）统一存储于数据库与文件系统，"
        "支持按用户查询回看；",
        "（5）全程遵循软件工程规范：界面展示完整、命名与注释规范、使用 Git 进行团队版本控制、"
        "编写规范的文档与测试记录。",
    ]:
        add_body(doc, t)

    # ---------------- 1.2 ----------------
    add_h2(doc, "1.2 系统分析")
    add_h3(doc, "1.2.1 可行性分析")
    add_body(doc,
             "技术可行性：单目标跟踪是机器视觉领域的经典问题。OpenCV 提供了成熟的 CSRT、KCF 等"
             "相关滤波跟踪器实现，不需要深度学习训练过程，一台普通 PC 即可达到实时处理速度；"
             "微信小程序开发生态成熟，Spring Boot + MyBatis + MySQL 是目前 Web 开发的主流技术栈，"
             "本机已具备 Java 17、Maven、Python 3.13（含 OpenCV 4.10）与 MySQL 8.0 环境，开发"
             "与运行环境齐备，技术路线完全可行。")
    add_body(doc,
             "经济可行性：本项目全部采用免费开源组件（OpenCV、Spring Boot、MySQL、微信开发者"
             "工具等），无额外软件采购成本；所需的开发设备为普通个人计算机，经济上完全可行。")
    add_body(doc,
             "操作可行性：小程序端与 Web 管理端界面均采用简洁、易操作的设计，普通用户只需完成"
             "选择视频与提交两步操作即可获得跟踪结果，管理员管理操作均有明确提示，系统易学易用，"
             "操作上可行。")

    add_h3(doc, "1.2.2 功能需求分析")
    add_body(doc,
             "依据任务书对“用户管理、视觉处理、图片/视频的存储和查询”的功能要求，结合单目标"
             "跟踪场景，系统功能需求如表1-1所示。")
    add_table(doc, "表1-1 系统功能需求列表",
              ["功能模块", "编号", "功能描述", "角色"],
              [
                  ["用户注册", "F-01", "填写用户名/密码/昵称完成注册，用户名唯一，密码长度不少于6位",
                   "普通用户"],
                  ["用户登录", "F-02", "用户名+密码登录，签发 JWT 令牌", "普通用户/管理员"],
                  ["修改密码", "F-03", "校验原密码后修改密码", "普通用户"],
                  ["视频上传", "F-04", "选择本地视频上传，可指定首帧目标框或自动选择", "普通用户"],
                  ["跟踪处理", "F-05", "服务端视觉模块执行单目标跟踪与遮挡重检测", "系统"],
                  ["结果回看", "F-06", "查询任务状态、播放结果视频、查看跟踪统计", "普通用户"],
                  ["历史记录", "F-07", "分页查看本人历史任务记录", "普通用户"],
                  ["用户管理", "F-08", "用户查询/新增/删除/重置密码", "管理员"],
                  ["任务管理", "F-09", "任务记录查询（按状态过滤）/删除", "管理员"],
                  ["系统统计", "F-10", "用户数、任务数及处理状态统计", "管理员"],
              ],
              col_widths=[2.6, 1.6, 7.4, 2.4])

    add_body(doc,
             "系统总体用例图如图1-1所示，普通用户可进行注册、登录、修改密码、上传视频、查看处理"
             "结果与历史记录等操作；管理员在上述基础上还可进行用户管理、任务记录管理与系统统计。")
    add_fig(doc, F("fig1_2_系统用例图.png"), "图1-1 系统总体用例图")
    add_body(doc,
             "结合任务书给定的系统流程（用户登录 → 提交视频/图片 → 服务器端视觉处理 → 返回结果 → "
             "结果入库 → 管理端管理），系统整体处理时序如图1-2所示：用户上传视频后后端异步调度视觉"
             "服务，视觉服务完成跟踪后将结果回传后端并入库，前端通过轮询获取最终结果。")
    add_fig(doc, F("fig1_3_系统处理时序图.png"), "图1-2 系统处理时序图")
    add_body(doc,
             "以核心的“视频上传与跟踪处理”为例，其用例描述如表1-2所示（参照报告模板表1.1格式）。")
    # 用例描述两列表格
    cap = doc.add_paragraph()
    cap.alignment = WD_ALIGN_PARAGRAPH.CENTER
    cr = cap.add_run("表1-2 “视频上传与跟踪处理”用例描述")
    cr.font.size = Pt(10.5)
    rows2 = [
        ("用例名：", "视频上传与跟踪处理"),
        ("标识号：", "UC-001"),
        ("参与者：", "普通用户"),
        ("用例描述：", "用户选择一段视频并指定（或自动选择）首帧目标对象，提交后由服务端视觉"
         "模块完成单目标跟踪，返回带跟踪框的结果视频与跟踪统计信息。"),
        ("前置条件：", "用户已登录系统。"),
        ("后置条件：", "任务状态更新为 SUCCESS（或 FAILED），结果文件已存储并生成访问路径。"),
        ("基本操作流程：", "① 用户在小程序端登录；② 选择要跟踪的视频；③ 选择“自动目标”或输入"
         "首帧目标框 bbox；④ 点击提交，系统返回任务 ID；⑤ 视觉模块异步处理，前端轮询任务状态；"
         "⑥ 处理完成后展示结果视频与“丢失/重捕获”统计。"),
        ("备选操作流程：", "若视频格式不支持或目标框非法，系统提示上传失败；若目标全程丢失未能"
         "找回，任务状态置 FAILED 并记录原因。"),
    ]
    t = doc.add_table(rows=len(rows2), cols=2)
    t.style = "Table Grid"
    for i, (k, v) in enumerate(rows2):
        c0, c1 = t.cell(i, 0), t.cell(i, 1)
        p0 = c0.paragraphs[0]; r0 = p0.add_run(k); r0.bold = True; r0.font.size = Pt(10.5)
        p1 = c1.paragraphs[0]; r1 = p1.add_run(v); r1.font.size = Pt(10.5)

    add_h3(doc, "1.2.3 非功能需求分析")
    add_body(doc,
             "性能需求：视觉模块对 640×480 分辨率视频的处理速度应达到实时或准实时（不低于 25 帧/秒）；"
             "上传接口对视频文件大小限制为 200MB。安全性需求：用户密码使用 BCrypt 加盐哈希存储，"
             "业务接口统一使用 JWT 令牌鉴权，管理端接口需要 ADMIN 角色权限。可靠性需求：视觉处理采用"
             "异步执行与状态机管理（PENDING→PROCESSING→SUCCESS/FAILED），任一环节失败均有明确错误"
             "记录，不阻塞用户其他操作。易用性需求：小程序与管理端界面层级不超过三层，关键操作均有"
             "提示与确认。")

    # ---------------- 1.3 ----------------
    add_h2(doc, "1.3 关键问题及解决办法")
    add_body(doc,
             "本系统开发中涉及的关键问题及其解决办法分析如下：")
    add_table(doc, "表1-3 系统关键问题及解决办法",
              ["关键问题", "问题分析", "解决办法"],
              [
                  ["跟踪算法选型与实时性",
                   "需兼顾跟踪精度与处理速度，任务书要求“基于视频流实时跟踪”",
                   "对比 KCF/CSRT 等主流跟踪器：KCF 速度快但尺度不变性差，CSRT 精度高、支持"
                   "尺度变化。本系统选用 CSRT 为主跟踪器并支持 KCF 切换，实测约 95~101 帧/秒，"
                   "满足实时性要求。"],
                  ["目标被遮挡后的再捕捉",
                   "遮挡时相关滤波模板被背景污染，直接恢复跟踪会丢失目标",
                   "设计“TRACKING/SEARCHING”双状态机：跟踪期间周期性用 HSV 直方图相关度与越界"
                   "检查判断是否丢失；丢失后进入搜索模式，以初始参考模板进行多尺度匹配并用直方图"
                   "验证，连续 2 帧命中即重新初始化跟踪器，实现再捕捉。"],
                  ["视频大文件上传与长任务处理",
                   "视频文件可达数十~数百 MB，视觉处理耗时长，同步接口易超时",
                   "后端提供 multipart 上传接口并异步线程池调度视觉处理；前端以任务 ID 轮询状态，"
                   "处理完成后再拉取结果，避免长连接阻塞。"],
                  ["三端数据流与结果共享",
                   "小程序、Web、视觉服务之间数据格式与文件访问方式不统一",
                   "统一 REST + JSON 契约；结果文件统一落盘到后端存储目录，通过 /files/** 静态"
                   "映射对外提供 URL，三端只需访问统一地址。"],
                  ["任务失败的可观测性",
                   "视觉服务异常、文件损坏等原因会导致任务中断",
                   "任务表记录 error_msg 与 finish_time，状态置 FAILED；管理端与小程序均可查看"
                   "失败原因，便于定位与重试。"],
              ],
              col_widths=[3.2, 4.4, 6.4])


# ============================ 第2章 ============================
def build_ch2(doc):
    add_h1(doc, "2 项目管理")
    add_h2(doc, "2.1 组员分工")
    add_body(doc,
             "本项目由 2 人组队完成，采用 Git 作为版本控制工具进行协作。为降低模块间耦合、"
             "便于并行开发，按“三端 + 数据”划分工作包，分工如表2-1所示。")
    add_table(doc, "表2-1 团队成员分工表",
              ["成员", "团队角色", "主要职责", "对应代码目录"],
              [
                  ["【姓名1·本人】", "组长", "视觉跟踪模块（Python+OpenCV）设计实现与算法调优；"
                   "Web 后端（Spring Boot）设计与实现；三端接口联调与系统测试",
                   "01-代码/vision、01-代码/server"],
                  ["【姓名2】", "成员", "微信小程序端全部页面与交互实现；Web 管理端页面实现；"
                   "数据库表结构设计与测试数据构造",
                   "01-代码/miniprogram、server/static"],
              ])
    add_body(doc,
             "协作过程说明：开发前共同编写接口约定文档（REST 路径、参数、返回格式）；开发中以"
             "Git 分支进行功能开发，各自完成后合并到主分支并解决冲突；联调阶段统一在本地环境按"
             "“视觉服务 → 后端 → 小程序/管理端”顺序启动，逐接口验证。")

    add_h2(doc, "2.2 项目进度控制")
    add_body(doc,
             "项目周期共安排 8 周，各阶段计划与时间安排如表2-2、图2-1所示。开发过程中按里程碑"
             "（视觉模块可运行、后端接口可调用、三端全流程贯通、测试完成）进行进度检查，实际进度"
             "与计划基本一致。")
    add_table(doc, "表2-2 项目进度计划表",
              ["阶段", "起止时间", "主要工作", "里程碑产出"],
              [
                  ["第1周", "第1周", "需求分析、方案设计、技术调研", "设计文档、任务分解"],
                  ["第2周", "第2周", "环境搭建、数据库设计", "建库脚本、工程骨架"],
                  ["第3~4周", "第3~4周", "视觉跟踪模块开发（CSRT+遮挡重检测）",
                   "可运行跟踪器与测试视频"],
                  ["第5周", "第5周", "Web 后端与管理端开发", "REST 接口、管理页面"],
                  ["第6周", "第6周", "小程序端开发", "小程序全部页面"],
                  ["第7周", "第7周", "三端集成联调、缺陷修复", "全流程贯通"],
                  ["第8周", "第8周", "系统测试、报告撰写、答辩准备", "测试记录、报告、PPT"],
              ],
              col_widths=[2.0, 1.8, 6.0, 4.2])
    add_fig(doc, F("fig2_1_项目进度甘特图.png"), "图2-1 项目进度甘特图", width_cm=14.0)

    add_h2(doc, "2.3 质量保障")
    add_body(doc,
             "为保证代码与文档质量，项目组制定了以下质量保障措施：")
    add_body(doc,
             "（1）编码规范：类名、方法名、变量名采用规范命名（如 SingleObjectTracker、"
             "processVideo）；关键逻辑添加注释；每个源文件头部注明创建时间、作者（学号姓名）与"
             "功能描述，符合任务书“在适当的位置添加注释，反映自己的学号姓名”的要求；")
    add_body(doc,
             "（2）版本控制：使用 Git 管理全部代码，按功能提交并书写规范提交信息，典型提交记录"
             "如表2-3所示；")
    add_table(doc, "表2-3 Git 提交记录（示例，以实际仓库为准）",
              ["提交说明", "涉及模块", "提交人"],
              [
                  ["feat(vision): CSRT 跟踪器与遮挡重检测状态机", "vision/tracker.py", "【姓名1】"],
                  ["feat(server): 认证与用户管理接口(JWT+BCrypt)", "server 后端", "【姓名1】"],
                  ["feat(mini): 上传跟踪与结果轮询页面", "miniprogram", "【姓名2】"],
                  ["feat(admin): Web 管理端用户/任务管理页面", "server/static", "【姓名2】"],
                  ["fix(vision): 完全遮挡场景无法触发丢失的缺陷", "vision/make_demo_video.py", "【姓名1】"],
                  ["test: 全链路功能测试与测试记录", "demo、文档", "【姓名1】【姓名2】"],
              ],
              col_widths=[6.6, 4.4, 3.0])
    add_body(doc,
             "（3）评审与自查：每个模块完成后由对方交叉检查代码（命名、异常处理、接口一致性），"
             "联调发现的缺陷统一登记并修复；")
    add_body(doc,
             "（4）文档保障：按模板格式同步撰写课程设计报告，测试过程如实记录，保证“代码—测试—"
             "文档”三者一致。")

    add_h2(doc, "2.4 系统集成")
    add_body(doc,
             "系统集成按“自底向上”策略分三步进行：第一步，单独验证视觉模块（命令行/接口方式对"
             "标准测试视频完成跟踪并输出结果）；第二步，验证后端与视觉服务的接口对接（track_local），"
             "以及后端与 MySQL 的数据读写；第三步，完成“小程序→后端→视觉服务→数据库→管理端”的"
             "全链路串联，冒烟测试结果如表2-4所示。")
    add_table(doc, "表2-4 系统集成冒烟测试记录",
              ["集成项", "验证方式", "结果"],
              [
                  ["视觉服务自检", "GET /api/v1/health 返回 ok", "通过"],
                  ["视觉处理接口", "POST /api/v1/track_local 对测试视频处理成功", "通过"],
                  ["后端-数据库", "注册/登录后查询用户信息一致", "通过"],
                  ["上传全链路", "小程序模拟上传 → 状态轮询至 SUCCESS → 结果视频可访问", "通过"],
                  ["管理端接口", "admin 登录后用户/任务/统计接口正常", "通过"],
                  ["结果文件服务", "GET /files/results/*.mp4 播放正常", "通过"],
              ],
              col_widths=[3.6, 8.0, 2.4])
